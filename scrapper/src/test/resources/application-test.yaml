persistence:
  type: jpa

api:
  bot:
    url: http://localhost:9090
  github:
    url: http://localhost:9090
  stack-overflow:
    url: http://localhost:9090


spring:
  datasource:
    url: jdbc:postgresql://localhost:65000/scrapper
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
app:
  github-token: token
  message-transport: Kafka

rate-limit:
  whitelist:
